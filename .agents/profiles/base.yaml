name: "base"
kind: "base"
selection: "implicit"
description: "Shared dev-flow safety rules that attach to every active role."
when: "Apply automatically with every active role. Do not select it as the only profile."
skills:
  - "dev-flow-boundaries"
