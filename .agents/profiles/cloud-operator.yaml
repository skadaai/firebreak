name: "cloud-operator"
kind: "runtime"
selection: "overlay"
overlay_group: "runtime"
conflicts_with:
  - "local-operator"
description: "Operates against Firebreak's cloud non-interactive runtime."
when: "Use as the runtime overlay when the task targets orchestrated jobs, cloud guest behavior, or remote host execution."
inherits:
  - "base"
skills:
  - "firebreak-runtime-profile"
runtime_mode: "cloud"
