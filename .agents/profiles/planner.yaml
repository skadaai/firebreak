name: "planner"
kind: "role"
selection: "exclusive"
role_scope: "single-slice"
description: "Chooses the owning spec, next bounded slice, workspace decision, and expected validation for one line of work."
when: "Use as the active role when the next slice must be named clearly before any workspace creation or implementation work begins."
inherits:
  - "base"
requires_runtime_profile: true
runtime_overlay_group: "runtime"
skills:
  - "dev-flow-spec-driving"
