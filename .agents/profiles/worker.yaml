name: "worker"
kind: "role"
selection: "exclusive"
role_scope: "single-slice"
description: "Implements one bounded Firebreak slice inside an active task."
when: "Use as the active role when a bounded slice is ready for code changes."
inherits:
  - "base"
requires_runtime_profile: true
runtime_overlay_group: "runtime"
skills:
  - "firebreak-work-task"
  - "firebreak-change-loop"
  - "firebreak-validation"
