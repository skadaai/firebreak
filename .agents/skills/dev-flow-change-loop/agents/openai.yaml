interface:
  display_name: "Dev Flow Change Loop"
  short_description: "Run the bounded dev-flow attempt loop"
  default_prompt: "Use $dev-flow-change-loop to execute a bounded change attempt with plan, validation, review, and stop conditions."
policy:
  allow_implicit_invocation: false
