# Local Runtime

- Target developer-operated VMs launched from the local wrapper.
- Interactive maintenance flows are valid.
- Host `PWD` sharing and local ergonomics are part of the contract.
- Manual shell access is acceptable when the task requires it.
- Smoke and test runs prefer isolated instance state over shared singleton state.
