interface:
  display_name: "Dev Flow Spec Driving"
  short_description: "Turn repo specs into small execution slices"
  default_prompt: "Use $dev-flow-spec-driving to turn a repo spec into the next bounded slice and choose the right workspace scope."
policy:
  allow_implicit_invocation: false
