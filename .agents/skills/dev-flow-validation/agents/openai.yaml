interface:
  display_name: "Dev Flow Validation"
  short_description: "Run named validation and classify outcomes"
  default_prompt: "Use $dev-flow-validation to choose the smallest sufficient validation suites and classify the result."
policy:
  allow_implicit_invocation: false
