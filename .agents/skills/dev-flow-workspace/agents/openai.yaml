interface:
  display_name: "Dev Flow Workspace"
  short_description: "Operate inside isolated spec-owned workspaces"
  default_prompt: "Use $dev-flow-workspace to keep autonomous work inside a spec-owned workspace. Reuse the workspace for sequential work on the same spec, and start a new workspace when the work moves to a different spec."
policy:
  allow_implicit_invocation: false
