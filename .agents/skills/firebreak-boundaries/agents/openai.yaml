interface:
  display_name: "Firebreak Boundaries"
  short_description: "Enforce Firebreak scope, limits, and shared state"
  default_prompt: "Use $firebreak-boundaries to keep a Firebreak task inside policy and write-scope limits."
policy:
  allow_implicit_invocation: false
